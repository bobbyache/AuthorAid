using System;
using System.Text;
using System.Collections.Generic;
using System.Linq;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using CygX1.AuthorAid.Windows.ViewModel;
using Moq;
using Domain.Services;

namespace ViewModelTests
{
    [TestClass]
    public class UnitTest_MainViewModel
    {
        [TestMethod]
        public void TestMethod1()
        {
            //var mock = new Mock<IRepository>();
            //mock.Expect
            //IRepository repository = new Repository
            //MainViewModel mainViewModel = new MainViewModel(

            //var projectServiceMock = new Mock<ProjectService>();
            //projectServiceMock.Setup(proj => proj.CreateNewProject("New Project", @"C:\AuthorAid", true)).

            //MainViewModel mainViewModel = new MainViewModel(null);
            //mainViewModel.
            //MainViewModel mainViewModel = new MainViewModel(null);
            //mainViewModel.
        }
    }
}
